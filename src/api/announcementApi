import { request } from './api';

const apiSection = '/posts/';

export async function getAnnouncement(userId) {
  const announcementsFromServer = await request(apiSection, { method: 'GET' });

  return announcementsFromServer;
}
